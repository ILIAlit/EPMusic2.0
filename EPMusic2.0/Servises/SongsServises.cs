using EPMusic2._0.Models;

namespace EPMusic2._0.Servises
{
    public class SongsServises
    {
        /*static void AddSongs()
        {
            // добавление данных
            using (ApplicationContext db = new ApplicationContext())
            {
                // создаем два объекта User
                Song song1 = new Song { Id = 1, Title = "dgdfgdg" };
                Song song2 = new Song { Id = 2, Title = "sgfxvsfxv" };

                // добавляем их в бд
                db.Songs.AddRange(song1, song2);
                db.SaveChanges();
            }
            // получение данных
            using (ApplicationContext db = new ApplicationContext())
            {
                // получаем объекты из бд и выводим на консоль
                var songs = db.Songs.ToList();
                Console.WriteLine("Users list:");
                foreach (Song song in songs)
                {
                    Console.WriteLine($"{song.Id}.{song.Title}");
                }
            }
        }*/
    }
}
